apiVersion: v1
kind: Service

metadata:
  name: flask-devops-service

spec:
  type: NodePort

  selector:
    app: flask-devops-app

  ports:
    - port: 80
      targetPort: 5000
      nodePort: 30080